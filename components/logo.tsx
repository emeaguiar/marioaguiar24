/**
 * Internal dependencies
 */
import { longCang } from '@/components/fonts';

export default function Logo() {
  return (
    <div className={longCang.className}>
      <h2 className='text-3xl transition-colors'>Mario Aguiar.</h2>
    </div>
  );
}
